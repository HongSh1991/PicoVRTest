using UnityEngine;
using System.Collections;

public class InputController : MonoBehaviour {

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	/*void Update () {
		//ABXY四个按钮
		if (Input.GetButtonDown(InputUtil.buttonX))
		{
			// PicoVRManager.SDK.ResetHead();
		}

		if (Input.GetButtonDown(InputUtil.buttonA))
		{
			
		}

		if (Input.GetButtonDown(InputUtil.buttonB))
		{
			Application.Quit();
		}	

		//方向键
		float keyVertical = Input.GetAxis("Vertical");  
		if(keyVertical == 1) //上
		{  
			
		}  
		else if(keyVertical == -1) //下
		{  
			
		}  

		float keyHorizontal = Input.GetAxis("Horizontal");  
		if(keyHorizontal == -1) //左
		{  

		}  
		else if(keyHorizontal == 1) //右
		{  

		}  
	}*/
}
